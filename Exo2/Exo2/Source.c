#include <stdlib.h>
#include <stdio.h>
#include <math.h>

int main(){
	long unsigned nb = 2868838400;
	printf("%lu est code sur %d octets et %d bits", nb, sizeof(nb), sizeof(nb)*8);

	for (int i = 0; i < sizeof(nb) * 8; i++) {

	}

}